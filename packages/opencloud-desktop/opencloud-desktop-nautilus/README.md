# opencloud-desktop-nautilus

Upstream's Python extension, installed with CMake. Only the Nautilus extension is installed; the upstream Nemo and Caja variants are excluded. Requires `opencloud-desktop` and `opencloud-desktop-integration-resources`, resolved inside the shared [`opencloud-desktop` COPR project](../README.md).

Restart Nautilus after installing it.
