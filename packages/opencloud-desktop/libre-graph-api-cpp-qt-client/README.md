# libre-graph-api-cpp-qt-client

Builds from its `client/` CMake directory, and ships a shared library and a development subpackage. Upstream publishes version tags without GitHub Releases, so the definition tracks the highest version tag rather than the latest release.

This is the prerequisite the rest of the [`opencloud-desktop` project](../README.md) builds against.
