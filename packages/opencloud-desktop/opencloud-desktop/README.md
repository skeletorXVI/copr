# opencloud-desktop

Built from the upstream source release. The default OpenCloud updater and update notifications are disabled for the RPM installation. The experimental OpenVFS plugin is not enabled.

Requires `libre-graph-api-cpp-qt-client`, resolved inside the shared [`opencloud-desktop` COPR project](../README.md).
